name: Build Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    # تحديد مكان العمل داخل الفولدر الصحيح
    defaults:
      run:
        working-directory: ./islam/islamic-app-frontend

    steps:
      - uses: actions/checkout@v4

      # 1. تجهيز pnpm
      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      # 2. تجهيز Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
          cache-dependency-path: './islam/islamic-app-frontend/pnpm-lock.yaml'

      # 3. تسطيب المكتبات
      - name: Install Dependencies
        run: pnpm install --no-frozen-lockfile

      # 4. بناء الموقع
      - name: Build React App
        run: pnpm run build

      # 👇 الخطوة المنقذة: إنشاء فولدر الأندرويد لو مش موجود
      - name: Add Android Platform
        run: npx cap add android

      # 5. تحديث ملفات كاباستور
      - name: Sync Capacitor
        run: npx cap sync

      # 6. تجهيز الجافا
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # 7. بناء الـ APK (مع إعطاء صلاحية التشغيل)
      - name: Build APK
        working-directory: ./islam/islamic-app-frontend/android
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug

      # 8. الرفع
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: IslamicApp-Debug
          path: islam/islamic-app-frontend/android/app/build/outputs/apk/debug/app-debug.apk
