name: Build Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    # 👇 التعديل الجوهري: حددنا المسار الصحيح للمشروع
    defaults:
      run:
        working-directory: ./islam/islamic-app-frontend

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Dependencies
        run: npm install --legacy-peer-deps

      - name: Build React App
        run: npm run build

      - name: Sync Capacitor
        run: npx cap sync

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Build APK
        # بندخل جوه فولدر android اللي جوه مسار المشروع
        working-directory: ./islam/islamic-app-frontend/android
        run: ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: IslamicApp-Debug
          # المسار الكامل عشان يرفع الملف الصح
          path: islam/islamic-app-frontend/android/app/build/outputs/apk/debug/app-debug.apk
